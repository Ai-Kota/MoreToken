# T-002: 多 key 路由 + 健康检查 + 冷却

**允许修改文件**：internal/**, main.go, docs/**
**允许新增文件目录**：internal/

## 目标
把 T-001 的 config + provider + pool 拼成可路由的核心（FreeLLMAPI 蓝本的"同格式 fallback + 格式隔离"）：
1. 格式隔离路由（I3 不混池的前置）：/v1/messages 只选 anthropic 格式、/v1/chat/completions 只选 openai 格式，互不串选
2. 同格式 fallback：选中 provider 失败 → 链上下一同格式 provider，指数退避冷却（池级，非 key 级）
3. 层级选择：免费层任一 key 健康 → 全走免费；免费全冷却 → 显式付费链（I2）
4. 请求级重试安全边界（I7）：仅响应字节前可重试；流已开不重放

按 TEST-MATRIX §5 先写测试：
- `TestRouter_FreeFirst_PaidZero`（F1，I1+I3）
- `TestRouter_AllFreeUnavailable_PaidChain`（F2，I2）

## 验收标准（DoD）
- [x] Router：Format 隔离 + tier 优先（免费全冷却才落付费）
- [x] 同格式 fallback 链 + 池级冷却（provider 失败退避，恢复后回归）
- [x] 请求级重试走 I7 边界（响应字节前）；429 落到 pool 冷却
- [x] TestRouter_FreeFirst_PaidZero / TestRouter_AllFreeUnavailable_PaidChain 通过
- [x] 主项目 build+vet+-race 全绿

## 注意
- state 状态机（I4 确定性回归 / I5 防抖）是 T-004，本任务只做到"当下选哪层"，不做后台探测回归
- 不做协议端点/NATS（T-003）；本任务产出的是可被 T-003 调用的路由核心
