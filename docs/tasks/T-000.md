# T-000: SPT 模板接入

**允许修改文件**：docs/**, TASKS.md, .claude/settings.json
**允许新增文件目录**：docs/, templates/, scripts/, .claude/

## 目标
把 SPT（软件开发管理模板）引入 moretoken，建立任务/质量/进度管理规范。
引入后，重构工作（以 FreeLLMAPI 为蓝本）按 SPT 四阶段流水线推进。

## 背景
项目原为 v4.0"会话保持型路由网关"（已归档提交 15d8f68）。经第一性原理评估：
参考对象 OmniRoute auto 不稳定，转向以本机验证稳定的 FreeLLMAPI 为蓝本重构。
用户要求先引入 SPT 再动手，确保开发过程有任务门禁与质量验收。

## 验收标准（DoD）
- [x] init.sh 引入成功（CLAUDE.md/INDEX.md/templates/scripts/docs-constraints 就位 + ai-guard 编译通过）
- [x] assess-maturity 完成（41/100 → Level 2）
- [x] generate-settings 2 生成（启用 check-scope / check-read-before-write / env-protect）
- [x] TASKS.md 登记接入任务 + 存量任务
- [x] GOALS.md 建立（重构版目标与边界）
- [x] ARCHITECTURE.md 更新为重构版蓝图（FreeLLMAPI 蓝本）
- [x] begin-task / verify-task 流程验证可用

## 步骤
1. 归档 v4.0 成果（提交 15d8f68）
2. 移除废弃聚合管线（source/merge/model/output/oauth/web）
3. init.sh 引入 SPT 资产
4. assess-maturity → generate-settings 2
5. 登记 TASKS.md + 本任务
6. 建立 GOALS.md + 更新 ARCHITECTURE.md
7. verify-task T-000

## 注意
- 编译 ai-guard 需 `GOWORK=off`（AImlyForge 根 go.work 不含本项目 module）
- SPT settings 的 permissions 用 src/** 白名单，本项目 Go 结构是 internal/**，
  若 hooks 拦 internal 写入需调整 settings
