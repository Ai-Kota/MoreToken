# T-008: 前端 WorkBoard 订阅（跨仓待办）

> **状态：🟡 跨仓待办** — 本仓（moretoken）已交付全部后端侧 + 契约，
> 本任务在**外部仓 `E:/repos/aimly-console`** 执行，此处仅作登记，不在本仓门禁范围内。

**归属仓**：`E:/repos/aimly-console`（前端应用，依赖 `@aimly/design-system`）
**前置（已就绪）**：`tools/moretoken/docs/workboard-contract.md` + 后端 NATS 发布（T-005）

## 目标
让 WorkBoard（aimly-console）订阅 MoreToken 网关的 NATS 状态/事件，渲染：
1. **主状态灯**：`aimly.system.freetier.status` 的 `tier` 字段（free=绿 / paid=橙）
2. **池状态表**：`status.pools[]`（provider / 格式 / key 数 / 冷却数 / 退避）
3. **切换时间线**：`aimly.system.freetier.event`（reason 映射：全挂切付费 / 回归免费 / 抖动保持付费）

## 验收标准（DoD，在 aimly-console 仓内验证）
- [ ] 订阅两主题（`aimly.system.freetier.>`），离线时显示"总线离线"而非静默
- [ ] status 渲染主状态灯 + 池状态表
- [ ] event 渲染时间线（3 种 reason 各一种样式）
- [ ] 密钥安全：渲染层不出现任何 key 值 / 请求体
- [ ] 与契约 schema 对齐（契约变更时同步）

## 依赖与契约
- 契约权威：`tools/moretoken/docs/workboard-contract.md` §三 payload schema
- 订阅示例：契约 §四（nats.cli / TS 片段）
- 主题注册：`deploy/nats/NEURAL_BACKBONE.md` §4.5（已登记 2 条，🟡 NATS 可降级）

## 注意
- 本仓**不**写 aimly-console 本体代码（外部仓 + 前端，跨仓边界）
- NATS 降级：后端 CLI 缺失时不发消息，前端须能显示离线态（不卡死、不误报"全绿"）
