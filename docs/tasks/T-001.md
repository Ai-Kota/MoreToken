# T-001: Go 骨架：config 模型 + provider 抽象

**允许修改文件**：internal/**, config/**, main.go, docs/**
**允许新增文件目录**：internal/, config/, docs/

## 目标
以 FreeLLMAPI 为蓝本搭骨架地基：
1. 声明式 config（provider×key×model+tier）✅ 已完成
2. provider 抽象（OpenAI / Anthropic 兼容请求封装）
3. 多 key 轮换池（健康 key 选择）

## 验收标准（DoD）
- [x] config 模型：Provider（ID/base_url/format/tier/keys/models）+ 校验 + `env:` key 引用 + 单测通过
- [x] provider 抽象：OpenAI（/v1/chat/completions）+ Anthropic（/v1/messages）请求封装，SSE 流式支持（internal/provider）
- [x] 多 key 轮换池：健康 key 选择、失败冷却、恢复（internal/pool，RR + 429 冷却 + fake clock）
- [x] config.json 示例（agnes 多 key / deepseek 付费兜底）—— 接入真实 key 需环境变量 AGNES_KEY_1..3 / DEEPSEEK_PAID_KEY
- [x] 全量测试通过（主项目 build+vet+test -race 全绿；test-all.sh 已补主项目节；scripts/ 的 ai-guard 存量测试失败与本次改动无关）

## 备注
- config 模型已完成并随提交 477c1c1 入库（config.go / config_test.go / config.json / main.go 骨架）
- 多 key 轮换是 FreeLLMAPI 稳定核心（账号级限流由多 key 吸收）
- 项目 go.work 已建，`go test ./internal/...` 直接跑通
