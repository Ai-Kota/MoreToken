# T-003: 协议端点 + 切换留痕日志 + NATS 状态发布

**允许修改文件**：internal/**, main.go, docs/**
**允许新增文件目录**：internal/

## 目标
把 T-002 的 router 接到 `:8462`，让 Claude Code / OpenAI 客户端可直连：
1. `/v1/messages`（anthropic 协议）→ router.FormatAnthropic
2. `/v1/chat/completions`（openai 协议）→ router.FormatOpenAI
3. `/health` → 各 provider 池状态
4. `/v1/models` → 模型列表（按格式过滤）
5. 切换留痕日志（决策日志）：每次路由记 (tier, provider, key, reason, retried)

## 范围裁剪
- **NATS 发布不做**：纯标准库零依赖是硬约束（GOALS.md）；NATS 是外部依赖。
  留痕日志落内存 ring buffer，T-005 WorkBoard 接入时再经 HTTP/文件暴露。
  （原 T-003 描述里的"NATS 状态发布"降级为 T-005 的可选集成，不阻塞本任务。）
- 流式：透传 SSE（provider 已支持），端点层不解析，按字节流回。

## 验收标准（DoD）
- [x] /v1/messages + /v1/chat/completions 端点跑通（fake 上游）
- [x] /health 返回各池冷却/可用状态
- [x] 切换留痕日志：每次 Route 记录决策（ring buffer，可查询最近 N 条，/decisions 端点暴露）
- [x] 端点测试：Claude Code 形状的请求能完整往返（非流式 + 流式各一）
- [x] main.go 组装 config→router→http server 启动（冒烟：/health /decisions /v1/models 通，无凭据→503）

## 注意
- 端点层保持薄：协议壳剥壳 + 调 router.Route + 透传，不夹业务逻辑
- 密钥不进日志（pool 已只认 keyID）
