# T-006: 成本确定性不变量 → 故障注入测试矩阵

**允许修改文件**：docs/quality/TEST-MATRIX.md, docs/tasks/TASKS.md
**允许新增文件目录**：docs/quality/

## 目标
把"成本确定性不变量"（免费优先 / 付费兜底 / 不混池 / 确定性回归 / 防抖）
从设计层（ADR-003 / GOALS.md）落成**可证伪的测试矩阵**：
每个不变量 → 注入前提 → 功能×正常×空×错误×边界×时序 的格子 → 可判定断言。

这是上一轮分析（"MoreToken vs FreeLLMAPI 第一性原理"）的产出落地：
GTG 相对 FreeLLMAPI 的差异化（确定性状态机）必须有矩阵钉死，
否则"更优秀"停留在设计层。

## 验收标准（DoD）
- [x] docs/quality/TEST-MATRIX.md 建立（从模板复制 + 填写）
- [x] 不变量 I1-I8 定义清晰，每条有"一句话 + 对应 ADR 条目"
- [x] 功能 F1-F7 每格填注入手段 + 断言（时序列为主战场）
- [x] 契约 fixtures 清单（429/tool-stream/401）登记
- [x] 一键回归登记（test-all.sh）
- [x] 覆盖登记列先建（测试函数名预登记，状态 ⬜，随 T-002/T-004 打勾）

## 注意
- 本任务只写**矩阵（测试计划）**，不写测试代码本身。
  测试代码（fault-injection 的 Go 实现）属于 T-002/T-004 的实现范围。
- 矩阵按 SPT 规则是"编码前的计划单位"，先于 T-002/T-004 的编码存在。
