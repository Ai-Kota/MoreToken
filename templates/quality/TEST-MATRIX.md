# 功能测试矩阵（完整性定义）

> 复制到 `docs/quality/TEST-MATRIX.md` 并填写。**编码前**枚举功能，每格填对应测试。
> 这是"所有功能必须可测到"的权威清单。图例：✅ 已覆盖 · ⬜ 未覆盖（阻塞）· — 不适用

## 后端

| # | 功能 | 正常 | 空 | 错误 | 边界 | 时序 |
|---|------|:--:|:--:|:--:|:--:|:--:|
| 1 |  |  |  |  |  |  |
| 2 |  |  |  |  |  |  |

## 前端（如适用）

| # | 功能 | 正常 | 空 | 错误 | 边界 | 时序 |
|---|------|:--:|:--:|:--:|:--:|:--:|
| 1 |  |  |  |  |  |  |

## 契约 fixtures（真实捕获）

| # | Artifact | 固化路径 | 验证内容 |
|---|----------|---------|---------|
| 1 |  |  |  |

## 一键回归

```bash
# 每个项目必建 test-all 脚本，每次改动全量重跑
bash test-all.sh
```

## 原则

1. 契约优先：真实 artifact 固化为 fixture，测试从契约而非代码推断。
2. 新增功能 → 先加矩阵行 + 测试，再合并。
3. 覆盖率是下限不是目标（覆盖行 ≠ 值正确）。
